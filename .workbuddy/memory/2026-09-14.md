# 2026-09-14

## 第二轮审查修复（R01–R15）全部完成

依据 `docs/reviews/2026-09-13-project-audit-round2.md` 完成修复，关键改动：

- **R01** `apps/aiks-cli/src/main.rs` + `cli/mod.rs`：`RebuildState`→`rebuild_sync_index`（保知识），新增 `ResetData`→`reset_all_data`（破坏性，DELETE ALL 确认）。
- **R02** `commands.rs list_knowledge`：WHERE 与参数向量同步构建（`params_from_iter`），COUNT 同谓词、错误上抛。
- **R03/R04/R05** `sync/engine.rs` 重写 `sync_session`：本地 `sync_target.target_id` 决定 create/update（不再依赖空实现 `find_document_by_session`）；UNCHANGED 必须满足 target.synced_hash == 当前 hash 且 parser 一致；dry-run 不做任何 DB 写入；冲突基线改为远端 exportMdContent 的 `md:<sha256>`（存 target_hash，捕获于上次成功同步）；`set_aiks_attrs` 失败→FAILED（先 `record_target_doc` 记映射，重试走 update 不产生重复文档）；`mark_synced` 错误上抛；updateBlock 失败回退 create（映射被远端删除时自愈）。
- **R06** `lifecycle.rs set_runtime()`：更新 lib.rs 预注册的 `Arc<Mutex<Option<SiyuanRuntime>>>` 容器，不再重复 manage。
- **R07** `save_settings`：读入完整 Config 只改 UI 受控字段，tmp+rename 原子写回；`AiModelConfig` 新增 `auto_extract` 字段；desktop Cargo.toml 增加 toml 依赖。
- **R08** `lifecycle.rs`：抽出 `spawn_background_tasks`（初始同步+Watcher 事件循环+周期扫描）与 `start_watcher_for_engine`（尊重 watch_enabled）；external 分支与 SiYuan 启动失败分支接入同一调度器（失败分支仍初始化 engine）。
- **R09** `AiksEngine::sync_and_enqueue_extraction` 尊重 `ai.enabled/auto_extract`；新增 `backfill_pending_extractions`（建 run 且真正提交 PipelineJob）+ Tauri 命令 `backfill_extractions`；UI `sync_and_extract`、托盘、CLI daemon 全部改走 sync_and_enqueue_extraction。
- **R10** `ai_stage.rs`：删除吞错的 `parse_v3_result`，run/map_reduce 直接 `parse_v3_result_typed(...)?`；map_reduce 的 chunk 失败也传播。
- **R11** `cleaner.rs` 字符边界截断；错误预览用 `safe_preview`。
- **R12** `session_chunker.rs` 重写：tokens 按字符估算；单条超预算消息按预算拆成多个 chunk（内容全保留）；首条消息计入累计。
- **R13** `worker.rs`：permit 在 spawn 前获取；同 session in-flight 去重；失败阶段从错误前缀还原（不再覆盖为 UNKNOWN，且已 FAILED 不重复写）；`recover_interrupted_runs`（PROCESSING→DISCOVERED 重新入队）在 `AiksEngine::initialize` 调用；repo 新增 `requeue_processing_runs`。pipeline_job 表仍未接入（遗留）。
- **R14** `ProviderRegistry::discover_all_detailed`（按源保留成功/失败）+ `mark_missing_sessions` 只对扫描成功的源判缺失。
- **R15** `markdown.rs render` 最终整文档脱敏兜底；`build_document_path` 标题过 default_sanitizer。

验证：`cargo test --workspace` exit=0（96 core 单测 + 6 m1m2 + 15 review_repro + 9 新增 review_round2_fixes = 126 全过）；`npm run build` exit=0。新增测试文件 `crates/aiks-core/tests/review_round2_fixes.rs`（原缺陷探针改为断言修复后行为）。

遗留：真实 SiYuan/模型端到端验收未做；pipeline_job 持久队列表未接入生产路径；运行中 engine 未热更新配置（保存设置重启生效）。

## 开发模式启动验证

- 帮用户验证了免打包开发启动流程：`scripts/dev.ps1` 一键启动（内嵌 SiYuan 运行时已就绪于 `apps/aiks-desktop/src-tauri/resources/siyuan`，100MB kernel + stage + appearance；node_modules 已装）。`npx tauri dev` 同时起 Vite(1420) 与 aiks-desktop.exe，应用自动拉起内嵌 SiYuan-Kernel(6806)。Rust dev 编译约 3.5 分钟（冷），增量后十几秒。
- 环境注意：本机 Bash 工具损坏（sleep/dirname 缺失），PowerShell 输出常被吞——可靠模式是 PowerShell + `Out-File ... -Encoding ASCII` 写到 target/*.txt 再 Read；`Get-NetTCPConnection` 比 Test-NetConnection 可靠（Vite 只绑 [::1]，IPv4 探测会误报）。
- 已向用户演示完整关闭流程：Ctrl+C 停 tauri dev，`taskkill /IM SiYuan-Kernel.exe /F` 补杀内嵌 SiYuan；三个进程均已验证关闭。

## 处理中心失败诊断 + UI 修复

- **设置页 Switch 越界根因**：`SettingsPage.tsx Toggle` 圆点 span 为 absolute 但未设 left，button 内静态位置居中导致开启态 translate-x-4 冲出轨道。修复：轨道 w-10 h-5、圆点 left-0.5 top-0.5、开启态 translate-x-5（对称几何），加 shrink-0。
- **处理中心失败诊断**（查 `%LOCALAPPDATA%\AIKnowledgeSync\aiks.db`）：pipeline_run 共 600：READY 17 / RAW_ONLY 376 / DISCOVERED 199 / FAILED 7 / PROCESSING 1。6 个新失败均为「AI JSON parse failed: key must be a string」——内网 Qwen3.8-27B 输出 JS 风格 JSON（键名无双引号）；1 个旧失败为上下文超长（R12 已修）。另有 siyuan/FAILED_RETRYABLE 24（SiYuan 不可用期间的写入失败，下次同步自动重试）。
- **AI 解析修复**：ai_stage.rs `clean_json` 先剥离 `<think>` 块；`parse_v3_result_typed` 失败后走 `repair_json` 兜底（裸键名补引号、去尾逗号、无双引号时单引号转双引号）；`SYSTEM_PROMPT_V3` 加 JSON 硬性格式要求。新增 3 个回归测试，workspace 129 测试全过。
- **补跑入口**：`backfill_pending_extractions`（覆盖 DISCOVERED+FAILED）后端命令已有但前端从未调用；在 api 层（index.ts/tauri.ts/mock.ts）补 `backfillExtractions()`，ProcessingPage 加「重试失败并补跑队列」按钮。`npm run build` 通过。
- 图标重做任务用户要求跳过（已生成一张紫底书本+同步箭头候选图在 target/icon_draft/，未启用）。
- **追加**：用户确认所有模型调用都应关思考（项目内全部是结构化 JSON 提炼任务）；旧版 `prompts.rs` SYSTEM_PROMPT 也补了 `/no_think` + 输出约束。**教训**：往 `r#"..."#` 原始字符串插入内容时把 `"#;` 写进了字符串中间导致编译错误（prompts.rs 还把字符串拦腰截断），已修复并通过 cargo check——编辑 raw string 时必须先确认结束符位置。

## 知识 → SiYuan 同步（knowledge-first 目录）实现

- 用户确认方案：SiYuan 目录树只展示提炼知识（按分类归档在 AI Knowledge 笔记本 `/20 Knowledge/{分类}/`），原始 Session 全部迁到独立笔记本「AI Session Archive」，知识文档末尾用 `siyuan://blocks/{session_doc_id}` 深链回原文。
- 实现：① config 加 `siyuan.session_notebook_name`（默认 AI Session Archive）；② Sink 扩展 `ensure_notebook_named/ensure_session_notebook/get_doc_notebook/move_docs/query_sql/build_knowledge_path/set_knowledge_attrs`（ATTR_KIND=custom-aiks-kind）；③ 迁移 004_knowledge_sync.sql 建 `knowledge_sync_target` 表 + `KnowledgeSyncRepo`（record_target_doc 先记 doc id 防重试重复建文档）；④ 新渲染器 `renderer/knowledge.rs`（knowledge_item → Markdown，3 个单测）；⑤ SyncEngine session 写入改用 `ensure_session_notebook`；⑥ 引擎加 `sync_knowledge_to_siyuan`（hash 增量 + 远端编辑冲突保护 + 远端删除自愈重建）与 `migrate_sessions_to_archive`（getDocInfo/moveDocs 幂等迁移，按 hpath 父目录保持结构）；⑦ `sync_and_enqueue_extraction` 首尾挂钩（迁移 + 知识推送），Tauri 命令 `sync_knowledge_to_siyuan`。
- 注意：pipeline worker 完成新知识条目后不即时推 SiYuan，靠周期扫描/手动同步触发（每小时/启动/手动点同步）。
- `cargo check --workspace` 通过；`cargo test --workspace` 因沙箱拒绝执行测试二进制未跑——待用户批准或自行运行验证。
- 概览页自适应修复：删 OverviewPage.tsx 根容器 `max-w-2xl`。

## 测试修复：知识同步改动导致的 3 个回归失败

- 用户授权后重跑 `cargo test --workspace --no-fail-fast`：132 测试中仅 review_round2_fixes 的 r03/r04/r05 失败。
- 根因：知识同步改造后 SyncEngine 用 `ensure_session_notebook`（笔记本名 "AI Session Archive"）写 Session 原文，但该测试文件的 mock SiYuan 服务器只认识 "audit" 笔记本，createNotebook 又返回 `data:null` → 解析失败 → 所有 session 同步变 Failed（updated_count=0；r05 连 target 都没记录直接 unwrap panic）。
- 修复：mock 的 lsNotebooks 返回 audit + "AI Session Archive"(id nba) 两个笔记本，并新增 createNotebook 分支返回合法 notebook 对象。测试代码语义未动。
- 警告清理：`cargo fix --workspace --allow-dirty --all-targets`（41→5），再手删 sink/siyuan.rs 死代码（DEFAULT_KNOWLEDGE_NOTEBOOK 常量、CreateDocResult/SearchAttrResult/BlockInfo 结构体）。注意：第一次 Edit 删结构体报成功但实际未落盘（疑似与 cargo fix 写文件竞争），需 grep 复核后重删。
- 最终：132 测试全过、0 警告；临时输出文件已删。

## 清空 SiYuan 数据 + 知识库页加手动同步按钮

- 用户报告 SiYuan 数据"乱码"，要求清空 SiYuan 全部数据并重推。检查发现：迁移产生的结构其实正确（目录占位 10 AI Sessions/{source}/{year}/{month}），真文档标题正文均正常中文；用户观感的"乱码/错乱"更可能是批量 moveDocs 时大量空占位文档 + 目录树错乱观感。用户后来表示先不管乱码根因。
- 清空操作（SiYuan API removeNotebook）：删掉 "AI Knowledge"（949 docs）与 "AI Session Archive"（8 docs）两个 AIKS 笔记本；SiYuan 里剩一个用户自建"我的笔记本"（未动）。**注意坑**：aiks-desktop 运行时会干扰删除（疑似内核侧占用/重建竞态）——先停 aiks-desktop 再删即成功；PowerShell→curl.exe 原生传参会剥离 JSON 双引号（报 invalid character 'n'），必须用 Invoke-RestMethod 或文件 body；PowerShell 工具对网络调用偶发 exit 1 误报，操作是否生效要用 lsNotebooks 复查。
- 本地配套：跑 `cargo run -p aiks-cli -- rebuild-state --yes` 重置 sync_target/sync_run/source_file_state/content_hash（知识数据保留）→ 下次启动所有 session 重新判定为 NEW 干净重建到 AI Session Archive，34 条知识重推到 AI Knowledge。
- 知识库页（KnowledgeBasePageV3.tsx）头部新增「同步到 SiYuan」按钮：调后端已有 `sync_knowledge_to_siyuan` 命令，带 loading 旋转与结果反馈（新建/更新/无变化/冲突/失败计数），api 三层（index/tauri/mock）加 `syncKnowledgeToSiyuan()`。`npm run build` 通过。
- 状态：aiks-desktop（dev 实例）已被停止；SiYuan-Kernel 仍在运行（数据已清空）。用户重启 dev.ps1 后首次同步将全部干净重建。

## dev.ps1 报错排查 + 发现 CONFLICT 误判 bug（修复中）

- 实测 dev.ps1 本身正常：Vite 1420、Rust 编译、aiks-desktop、内嵌 SiYuan-Kernel 6806 全部成功拉起，首次同步开始重建文档。用户看到的"报错"疑似 PowerShell 5.1 把 npm/vite stderr 进度显示为红字 NativeCommandError 的噪音。
- **发现真 bug**：内嵌 SiYuan 内核没有 `/api/filetree/exportMdContent` 端点（返回纯文本 404）→ R05 基线捕获每篇失败 → fallback 把 content hash 存进 target_hash → 下次同步 `hash_markdown(remote)`（带 md: 前缀）!= content hash → **全部 session 误判 CONFLICT**。修复方向：fallback 改存 NULL（engine 已有 None→跳过冲突检测语义），`SyncTargetRepo::mark_synced` 的 target_hash 改 Option<&str>；待用户手动跑完 dev 后修。
- 环境注意：SiYuan-Kernel 在 6806 返回 "upstream connect failed: os error 10061" 的原因——6806 归属进程是 `target\debug\resources\siyuan\kernel\SiYuan-Kernel.exe`（tauri dev 拷贝的运行时），与 `apps\aiks-desktop\src-tauri\resources\siyuan` 不是同一份；upstream 报错可能是内核内部服务未就绪/崩溃中。
- 当前状态：应求已把 aiks-desktop / SiYuan-Kernel / cargo / vite(node) 全部停止，1420/6806 均已释放，用户手动执行 dev.ps1，执行完再回来修 CONFLICT 误判 bug。

## 全清重建完成：create document 失败根因修复 + 全局同步锁

**根因链**：SiYuan workspace temp 膨胀 11.66GB（siyuan.db 7.86G + blocktree.db 2.88G + history.db 1.76G）+ C 盘仅剩 ~12GB → 单事务写入 5~67 秒 → 超过 sink 30s 超时 → 大量 `create_document: create document` 失败；且失败时不写 target_doc 映射但服务端事务实际完成 → 重跑重复创建。

**修复**（全部已验证）：
1. `sink/siyuan.rs`：两处 `Client::timeout` 30s → **300s**（容忍大事务写入）。
2. `engine/mod.rs`：新增 `sync_lock: tokio::sync::Mutex<()>` **全局同步互斥锁**——`sync()`（拆出无锁实现 `sync_unlocked`）、`sync_and_enqueue_extraction()`（锁内跑 migration+sync+bookkeeping）、`sync_knowledge_to_siyuan()` 全部持锁。根因：启动全量流与 watcher 流并发，双方都见 NULL target_id → 各自 create → **每 session 两份文档**。
3. 上一轮已修：exportMdContent→getBlockKramdown、target_hash 基线 Option 化、迁移幂等（pragma 守卫）。

**全清重建流程**（用户授权全删重来）：回收站精准清理 20 条 AIKS 条目释放 12.01GB（C 盘 24GB）；删 target/debug/incremental 7GB 损坏缓存（rustc ICE 消失）；`rebuild-state --yes` 清 sync 索引（保 knowledge_item）；删 workspace data 下 3 个笔记本目录 + temp；干净重启 dev 验证。

**验证结果**：cargo test --workspace 135 全过（105 core + 6 m1m2 + 15 review_repro + 9 review_round2）；dev 实测 662 session 同步健康推进约 1 条/秒，无 create document 错误、无重复创建（锁生效）。验证后已停掉我的实例，交用户手动启动。

**残留（非阻塞）**：启动 3 秒内 SiYuan SQL 索引未就绪 → "database is nil" migration WARN + 偶发 "block not found"/"tree not found" 瞬态失败，下次扫描自动重试自愈；1 个孤儿 session（rollout-2026-08-20T14-40-55 源文件已删）每次同步报 load_session 失败；C 盘剩 ~23GB，temp 重建后会再占数 GB。

## 数据根重定向：AIKS/SiYuan 整体迁至 E 盘

**需求**：用户要求把 SiYuan（含膨胀的 temp）移出 C 盘。

**实现**（aiks-core `config/mod.rs` + desktop `app_state.rs`）：
- 新增模块级 `Config::data_root()`：优先级 `AIKS_DATA_DIR` env var → `<默认根>\data-root.txt` 指针文件 → 默认 `%LOCALAPPDATA%\AIKnowledgeSync`。纯函数 `resolve_data_root` 带 4 个单测。
- `state_db_path()` / `archive_dir()` / desktop `app_state::data_dir()` 全部改走 `data_root()`，三处硬编码归一；CLI 自动跟随（`Config::default()`）。
- 踩坑：`data_root` 先放 `impl Config` 里导致 desktop 以模块路径调用 E0425，移为模块级 pub fn；中间一次 Edit 造成游离方法段，需检查 impl 块闭合。

**数据迁移**（robocopy 复制 → 校验 → /MIR 清源）：
- `C:\...\AIKnowledgeSync` → `E:\AIKS-Data`：2251 文件 4.37GB 字节级一致；aiks.db 连同 WAL/SHM、siyuan workspace 完整。
- pointer 文件 `data-root.txt`（内容 `E:\AIKS-Data`）留在 C 盘原目录作为引导。
- **重要**：robocopy /MIR 的删除也会被宿主 safe-delete 钩子转进 C 盘回收站（77 条 AIKS 条目），C 盘不释放反降——用户手动清了回收站，最终 C 盘 27.49GB（净释放 ~4.5GB）。

**验证**（我的 dev 实例实测后已停，交用户手动启动）：
- `Data directory: E:\AIKS-Data`、`Kernel args: --workspace=E:\AIKS-Data\siyuan\workspace` ✓
- 笔记本复用（Notebook already exists）——迁移 workspace 被完整识别 ✓
- 已同步 session 全部 Unchanged（迁移后内容一致），未同步的继续 Updated 推进 ✓
- 109 core 测试全过。
- 注意：我后台会话里 WebView2 创建失败（0x80070578 没有注册类）但后端同步正常——疑与无桌面交互的服务会话有关，用户桌面会话待确认。

## 知识同步落地：CLI sync-knowledge 命令 + upsert bug 修复 + 34 条知识同步成功

**用户疑问**：打开 6806 只看到 "AI Session Archive"，没看到知识库样式（"r语言"为语音输入误差，指 AISK）。

**查明事实**：AI Session Archive = 原始 session 归档（160.7MB，按设计）；知识库样式在 "AI Knowledge" 笔记本，但此前为空——知识同步只能从桌面知识库页「同步到 SiYuan」按钮或 `sync_knowledge_to_siyuan` 触发，从未执行过。查库：knowledge_item 34 条（implementation 14 / troubleshooting 8 / configuration 7 / general 2 / architecture 1 / decision 1 / research 1），knowledge_sync_target 0 行。

**实现**：CLI 新增 `SyncKnowledge { overwrite_conflicts }` 命令（cli/mod.rs + main.rs）。

**修复**：`storage/repo.rs` 4 处 UPSERT（record_target_doc / mark_synced / mark_conflict / mark_failed）`ON CONFLICT(knowledge_id, sink)` → `ON CONFLICT(knowledge_id)`——表实际是单列 `knowledge_id TEXT PRIMARY KEY`，复合子句直接报 "does not match any PRIMARY KEY or UNIQUE constraint"。**坑**：SiYuan 端文档实际已创建（API 节奏 1-2 秒/条）而映射写入失败 → 需删除误建文档的 AI Knowledge 笔记本（removeNotebook）再重跑，否则重复。

**结果**：`created=34 failed=0` 全部成功；刷新 6806 可见 /20 Knowledge/{项目}/{分类} 树；每条知识文档末尾有「原始工作记录：siyuan://blocks/{doc_id}」深链跳原始 Session。

**其他**：用户已手动清回收站；单独启动内核报 lock workspace——用户自己的 SiYuan-Kernel（pid 6592）占用 6806 与 E 盘 workspace，不杀用户进程直接复用执行同步。

## 知识→Session 快捷跳转：块引用改造 + getDocInfo 缺失修复（34/34 完成）

**需求**：用户要"快捷跳转到原始 session"。查明：① 原链接是 `siyuan://` 外链 markdown（浏览器里点击行为不可靠）；② 34 条中仅 20 条有链接——14 条的 session 文档在知识同步**之后**（18:26-18:33）才同步完成，当时 status 非 SYNCED 解析不到。

**改造**（renderer/knowledge.rs）：跳转改为思源**原生块引用** `((doc_id "点击查看原始 Session"))`，放在文档**开头元信息第一行**（不用滚页底，单击即跳、进反向链接面板）；页底改为纯文本「原始工作记录：{标题} + Session ID」。block_ref() 对 label 做引号中和。测试更新后 140 全过。

**新 bug**：`sink.get_doc_notebook` 用 `/api/filetree/getDocInfo`——内嵌内核**没有该端点**（纯文本 404→JSON 解析失败）。首跑 create 路径不调用它所以 18:26 成功；update 路径必调 → 34 条全失败。重写为 query_sql：`SELECT box FROM blocks WHERE id='..' AND type='d' LIMIT 1`（通用、防注入）。**教训**：嵌入内核端点子集不全，新端点先用 curl 探测。

**补跑过程**：第一轮 updated=18 / failed=16——恰逢用户关闭 dev 实例、内核关库瞬间（日志可见我的 SQL 撞上 database is closed）；临时拉内嵌内核补完 updated=16 → **34/34 均含块引用**（ASCII 模式 `instr(markdown,'((2026')` 验证）后停止内核。

**环境坑**（重要）：
- Start-Process 拉起的内核在工具调用结束后**被沙箱回收**（进程静默消失）——要常驻必须用 Bash run_in_background 任务，结束时 TaskStop。
- **中文经 PowerShell→Invoke-RestMethod 请求体进 SiYuan SQL 会被编码损坏**，LIKE/instr 中文模式永远 0 命中；验证必须用 ASCII 模式（如 `'((2026'`、`'Session'`）。
- 编译慢根因：用户 tauri dev 与我的 cargo 共用 target 目录（构建锁排队 + 我改 aiks-core 触发其 watch 全量重编桌面应用 + incremental 缓存 os error 5）；另 cargo test 编 4 个集成测试二进制全量链接。只跑单测用 `cargo test -p aiks-core --lib` 可大幅提速。
- 用户 dev 实例已关闭，当前无 AISK/SiYuan 进程；数据一致，下次启动无需任何修复。
