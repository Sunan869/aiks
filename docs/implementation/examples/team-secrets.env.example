# 只保留空值，不含真实凭据。真实 .env 不提交 Git。
# 原生 Service 不自动执行/加载 .env。由 Compose env_file 或服务管理器注入。
# 设计阶段参考；正式部署空模板见 deploy/team/.env.example。真实值不得提交 Git。
AIKS_DINGTALK_CLIENT_SECRET=
AIKS_SIYUAN_TOKEN=
AIKS_AI_API_KEY=
AIKS_EMBEDDING_API_KEY=
