# S1 final acceptance review

Review type: author self-review; no independent agent/reviewer was available. No main merge or Release is requested. Read this with aiks-service-s1-progress.md and aiks-service-s1.md; it does not widen S1 to S2–S4.

Reviewed boundaries: native mode selection before legacy engine startup; single business writer; client-only providers/outbox; immutable target and revision; guarded derived writes; trusted scoped recall before limits; canonical content reads; owner-only process controls; profile/config path validation; opt-in model endpoints; non-mock Service UI; one-command Windows dev orchestration; recovery and isolated offline integration.

Important findings addressed with regression coverage: bounded collector could permanently skip later sessions; runtime lookup could install legacy bridge while starting a new space; startup and shutdown could race ownership cleanup; tray actions could select the obsolete GUI; the UI implied all model traffic stayed local. The original implementation tests failed before fixes. Native startup cancellation/config tests and the full Linux workspace also pass at095bc89.

The closing formatting/docs checkpoint305aea passed the pre-existing product gates inspected to date but exposed a TEST-HARNESS scoping bug: global mock functions called from a different PowerShell script could not access the test's script:events variable. Job106700613590 failed at the first fake setup command before exercising dev.ps1. Fix the test state to a uniquely named disposable-process global, not production dev.ps1, and retain the original orchestration assertions. The intentionally simulated failed cargo call must restore LASTEXITCODE for the test process after verifying startup is rejected; do not suppress a real build failure.

Expected final checks: exact final HEAD cargo fmt/locked Core/Service tests, workspace Clippy/tests, Linux+Windows native client/lifecycle tests, Windows PowerShell5.1 entrypoint contract, frontend tests/build and network-isolated model/content fixture integration. Preparing formatted artifacts is not a substitute. Read-only verification workflows remain; the generated-blob writer workflow has been removed.

Deferred/explicit boundaries: list paging/JSON session detail and TOML-only model settings are basic S1 UX; real divergent versions stay blocked for explicit human reconciliation; no automatic old-data migration; old full editor remains available through -Legacy; public content write/team sharing/RAG and installer work belong to later stages. Existing npm audit warnings are release-hardening debt and prevent any blanket security-audit claim. Model quality, real installation formats, graphical UX and large-dataset performance still require local acceptance.
